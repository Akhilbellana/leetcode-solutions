            if (j - i + 1 == l) {
                if (sum > 0) {
                    min = Math.min(min, sum);
                }
                i++;
                sum -= nums.get(i);
                sums = sum;
                    sums += nums.get(x);
                int x = j+1;

                }
                    if (sums > 0) {
                        min = Math.min(min, sums);
                    }
                    x++;
            sum += nums.get(j);
        while(j < nums.size()) {
        int min=Integer.MAX_VALUE;
        int sums = 0;
        int j = 0;
                int y=0;
                while(y<r-l && x<nums.size()){
            }
            j++;
        }
        return min;
                    y++;
