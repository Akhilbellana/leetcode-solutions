                 right=j-1;
                while(left<right){
                     if(s.charAt(left)!=s.charAt(right)){
                        break;
                        validright=false;
                 left=i;
                }
                
                    right--;
                    left++;
                    }
                        break;
                        validleft=false;
                 boolean validright=true;
                    if(s.charAt(left)!=s.charAt(right)){
                while(left<right){
